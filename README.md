# file-finder
just find files in your computer

*file_finder_linux* find files in directory "/"  
*file_finder_win* find files in directory "C://"

Use a semicolon and a space (; ) to perform a multi-search.

Requirements:
1. python
